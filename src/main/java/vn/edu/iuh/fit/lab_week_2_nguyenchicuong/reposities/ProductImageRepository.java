package vn.edu.iuh.fit.lab_week_2_nguyenchicuong.reposities;import org.hibernate.Session;import org.hibernate.SessionFactory;import org.mariadb.jdbc.util.Security;import vn.edu.iuh.fit.lab_week_2_nguyenchicuong.connectDB.ConnectDB;public class ProductImageRepository {    private SessionFactory sessionFactory;    private Session session;    public ProductImageRepository() {        sessionFactory= ConnectDB.getInstance().getSessionFactory();    }    //Lấy ra đường dẫn ảnh của sản phẩm    public String getImageById(long id){        session=sessionFactory.openSession();        session.beginTransaction();        String path= (String) session.createQuery("select p.path from ProductImage p where p.product.product_id=:id").setParameter("id",id).getSingleResult();        session.getTransaction().commit();        session.close();        return path;    }}